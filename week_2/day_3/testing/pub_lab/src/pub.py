class Pub:
    
    def __init__(self, name, till):
        self.name = name
        self.till = till
        self.drinks = []

    # create function that adds drink to pub drinks

    # create function that gets drinks count

    def increase_sum(self, amount):
        self.till += amount

    #create a function that
    #checks if drink count is higher than 0 or else returns
    #reduces wallet by drink price
    #increases till by drink price
    #checks drinks count